
### DON'T add trailing slashes in dir names

# your images folder absolute/relative (to the app.py script) path
datadir: /iexec


# If you're not sure do not edit this section, it's used by the app

input-config: input-config.yml    # contains image names and text's language
input-dir: in                     # after the execution, input files will be moved to this folder
output-dir: out                   # output dir